# Hangullo Type System

## Hangullo 자료형 시스템 명세서

---

## 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | Hangullo Type System |
| 프로젝트 | Hangullo Programming Language |
| 버전 | v0.0.1-beta |
| 상태 | 초기 설계 문서 |
| 목적 | Hangullo 언어의 자료형(Type System) 정의 |

---

# 1. 개요

Hangullo Type System은 Hangullo 프로그래밍 언어에서 사용하는 자료형(Type)의 구조와 동작 원칙을 정의하는 공식 문서입니다.

자료형은 프로그램에서 데이터를 저장, 처리 및 전달하는 기본 단위이며, 언어의 안정성과 표현력을 결정하는 핵심 요소입니다.

본 문서는 Hangullo이 제공하는 자료형의 종류와 설계 방향을 정의하며, 세부 구현은 언어의 발전 과정에 따라 확장됩니다.

---

# 2. 설계 목표

Hangullo의 자료형 시스템은 다음 목표를 기반으로 설계됩니다.

- 직관적인 자료형 이름 제공
- 명확한 데이터 표현
- 예측 가능한 동작
- 높은 안정성
- 확장 가능한 구조

---

# 3. 자료형 분류

Hangullo의 다음과 같은 자료형을 제공합니다.

| 분류 | 설명 | 상태 |
|---|---|---|
| 기본 자료형 (Primitive Type) | 언어에서 기본 제공 | 예정 |
| 참조 자료형 (Reference Type) | 객체 및 컬렉션 | 예정 |
| 사용자 정의 자료형 (User-defined Type) | 개발자가 정의 | 검토 중 |

---

# 4. 기본 자료형

## 4.1 정수(Integer)

정수 자료형은 소수점이 없는 숫자를 표현합니다.

예시

```
10
-25
1000
```

주요 특징

- 정수 연산 지원
- 비교 연산 지원
- 산술 연산 지원

---

## 4.2 실수(Float)

실수 자료형은 소수점을 포함하는 숫자를 표현합니다.

예시

```
3.14
-12.75
5
```

주요 특징

- 소수 계산 지원
- 산술 연산 지원
- 비교 연산 지원

---

## 4.3 문자열(String)

문자열 자료형은 문자들의 집합을 표현합니다.

예시

```
"안녕하세요"
"Hangullo Language"
```

주요 특징

- 문자열 연결
- 문자열 비교
- 길이 확인
- 문자 접근

---

## 4.4 논리(Boolen)

논리 자료형은 참(True) 또는 거짓(False)를 표현합니다.

예시

```
참
거짓
```

주요 특징

- 조건문
- 반복문
- 논리 연산

---

# 컬렉션 자료형

컬렉션은 여러 개의 데이터를 하나의 객체로 관리하는 자료형입니다.

---

## 5.1 목록(List)

목록은 여러 개의 데이터를 순서대로 저장합니다.

예시

```
[1, 2, 3]
```

지원 예정 기능

- 데이터 추가
- 데이터 삭제
- 데이터 수정
- 순회
- 검색

---

## 5.2 사전(Dictionary)

사전은 키(Key)와 값(Value)의 쌍으로 데이터를 저장합니다.

예시

```
{
    "이름": "홍길동,
    "나이": 20
}
```

상태

검토중

---

## 5.3 집합(Set)

집합은 중복되지 않은 데이터를 저장합니다.

상태

검토 중

---

# 6. 사용자 정의 자료형

Hangullo은 사용자가 새로운 자료형을 정의할 수 있도록 설계합니다.

예상 대상

- 구조체
- 클래스
- 열거형(Enum)

세부 문법은 별도 문서에서 정의합니다.

---

# 7. 자료형 변환

Hangullo은 자료형 간 변환(Type Conversion)을 지원합니다.

변환 방식

- 자동 변환(Implicit Conversion)
- 명시적 변환(Explicit Conversion)

자동 변환은 데이터 손실이 발생하지 않는 경우에 한하여 허용하는 것을 원칙으로 합니다.

---

# 8. 자료형 비교

같은 자료형끼리는 기본적으로 비교 연산을 지원합니다.

자료형이 다른 경우의 비교 규칙은 별도 명세에서 정의합니다.

---

# 10. Null 값

Hangullo은 값이 존재하지 않음을 표현하기 위한 Null 값을 지원할 수 있습니다.

Null 지원 여부 및 표현 방식은 추후 확정합니다.

현재 상태

검토 중

---

# 11. 불변성(Immutable)

자료형은 두 종류로 구분될 수 있습니다.

- 변경 가능한 자료형(Mutable)
- 변경 불가능한 자료형(Immutable)

각 자료형의 특성은 추후 정의합니다.

---

# 12. 메모리 모델

자료형의 저장 방식과 메모리 관리 방식은 Hangullo Memory Model 문서에서 정의합니다.

참고 문서

- Hangullo_Memory_Model.md *(예정)*

---

# 13. 향후 확장 예정

향후 다음 자료형을 검토합니다.

- 튜플(Tuple)
- 범위(Range)
- 바이트(Byte)
- 날짜(Date)
- 시간(Time)
- 함수(Fuction)
- 제네릭(Generic)

---

# 14. 참고 문서

- Hangullo_philosophy.md
- Hangullo_specification.md
- Hangullo_grammar.md
- Hangullo_keywords.md
- Hangullo_memory_model.md *(예정)*

---

# 15. 결론

Hangullo Type System은 Hangullo 프로그래밍 언어의 자료형 체계를 정의하는 공식 문서입니다.

자료형 시스템은 언어의 안정성과 표현력을 결정하는 핵심 요소이며, Hangullo의 발전 과정에 따라 지속적으로 확장 및 개선됩니다.

본 문서는 Hangullo 컴파일러, 표준 라이브러리 및 개발 도구 구현의 기준으로 사용됩니다.